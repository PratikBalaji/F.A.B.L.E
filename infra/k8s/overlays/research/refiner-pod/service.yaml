apiVersion: v1
kind: Service
metadata:
  name: refiner-pod
  namespace: fable
  labels:
    app: refiner-pod
    app.kubernetes.io/part-of: fable
spec:
  selector:
    app: refiner-pod
  ports:
    - name: http
      protocol: TCP
      port: 8004
      targetPort: 8004
  type: ClusterIP
