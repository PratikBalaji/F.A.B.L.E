apiVersion: v1
kind: Service
metadata:
  name: presidio-analyzer
  namespace: fable
  labels:
    app: presidio-analyzer
    app.kubernetes.io/part-of: fable
spec:
  selector:
    app: presidio-analyzer
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
